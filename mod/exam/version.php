<?php
   defined('MOODLE_INTERNAL') || die();

   $plugin->version   = 2025052807; // Sürüm: YYYYMMDDXX formatında.
   $plugin->requires  = 2022041900; // Gerekli Moodle sürümü (örneğin, Moodle 4.0).
   $plugin->component = 'mod_exam'; // Modül adı.
   $plugin->maturity  = MATURITY_ALPHA; // Modül olgunluk seviyesi.
   $plugin->release   = '1.0'; // Modül sürüm numarası.
   ?>